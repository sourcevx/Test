{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tasks list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Taskname</th>
                <th>Taskdescription</th>
                <th>Taskrun</th>
                <th>Taskend</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('task_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.taskName }}</td>
                <td>{{ entity.taskDescription }}</td>
                <td>{% if entity.taskRun %}{{ entity.taskRun|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.taskEnd %}{{ entity.taskEnd|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('task_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('task_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('task_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
