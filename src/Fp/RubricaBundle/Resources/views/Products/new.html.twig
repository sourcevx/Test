{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Products creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('products') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
