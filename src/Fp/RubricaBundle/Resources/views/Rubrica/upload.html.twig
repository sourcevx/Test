{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Upload File</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}
        <ul class="record_actions">
    <li>
        <a href="{{ path('rubrica') }}">
            Back to the list
        </a>
</ul>
{% endblock %}
