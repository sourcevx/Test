{% extends '::base.html.twig' %}
{% block body -%}
    <ul class="record_actions">
        <li>
            <a href="{{ path('rubrica') }}">
                Back to the list
            </a>
        </li>
    </ul>
    <h1>Ricerca Localita </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% endblock %}
